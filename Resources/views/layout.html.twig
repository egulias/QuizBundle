<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <link rel="stylesheet" href="{{ asset('bundles/quiz/css/quiz.css') }}" type="text/css" media="all" />
        <title>{% block title %}Quiz Bundle{% endblock %}</title>
        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" />
        <script src="{{ asset('js/jquery-1.7.1.min.js') }}" type="text/javascript"></script>
        {% block extra_js %}{% endblock %}
    </head>
    <body>
        <div id="quiz-wrapper">
          {% block navigation %}
            <p><a href="{{ path('egulias_quiz_question_add') }}">Add Questions</a></p>
            <p><a href="{{ path('egulias_quiz_add') }}">Add Quiz</a></p>
            <p><a href="{{ path('egulias_quiz_panel') }}">Quiz List</a></p>
          {% endblock %}
          {% block content %}content {% endblock %}
        </div>
    </body>
</html>
